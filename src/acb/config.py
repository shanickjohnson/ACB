"""Shared constants used across the API layer and the agent graph."""

# The Gemini model used for every chat/routing/translation call. Was
# hardcoded as a literal string in four separate modules; change it here.
CHAT_MODEL = "gemini-3.6-flash"
